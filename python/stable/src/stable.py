import numpy as np

class Stable(): 
    def __init__(self, alpha = 2.0, beta = 0.0, gamma = 1.0, delta = 0.0):
        self.alpha = alpha
        self.beta = beta
        self.gamma = gamma
        self.delta = delta